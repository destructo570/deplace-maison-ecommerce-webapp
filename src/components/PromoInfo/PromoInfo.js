import { StyledPromoInfo } from "./styledComponents";

function PromoInfo() {
  return (
    <StyledPromoInfo>
      <p>
        <span>Who we are</span>
        An independent <span>brand</span> of urban trekking shoes and
        accessories that comes from a convergence of arts and personalities.
      </p>
    </StyledPromoInfo>
  );
}

export default PromoInfo;
